apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: akhq
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`akhq.localhost`)
    kind: Rule
    services:
    - name: akhq
      port: 80